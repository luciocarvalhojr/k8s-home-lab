apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: go-api
  namespace: argocd
spec:
  destination:
    namespace: go-api
    server: https://kubernetes.default.svc
  sources:
    - repoURL: https://luciocarvalhojr.github.io/helm-charts
      chart: go-api
      targetRevision: 0.1.1
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true